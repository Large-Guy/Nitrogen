module program;

struct Transform
{
    f32<3> position;
    f32<3> rotation;
    f32<3> scale;
}

Transform newTransform(f32<3> position, f32<3> rotation, f32<3> scale)
{
    Transform transform;
    transform.position = position;
    transform.rotation = rotation;
    transform.scale = scale;
    return transform;
}

void main()
{
    let transform = newTransform((1,2,3), (0,0,0), (1,1,1));
}